/**
 * @file Configura PostCSS y Tailwind CSS para procesar los estilos.
 *
 * Este respaldo añade comentarios; la lógica ejecutable permanece sin cambios.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
